version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: astroqua
      POSTGRES_PASSWORD: astroqua123
      POSTGRES_DB: astroqua_ocean
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
